name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore BBSolutions.slnx

      - name: Build
        run: dotnet build BBSolutions.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test BBSolutions.slnx --no-build --configuration Release --verbosity normal
